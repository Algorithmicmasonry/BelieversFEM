export const getDashboardOverviewData =  async () => {
  // array of products with id, name, price, description, isActive, views, whatsappClicks,

  // mock analytics of total views, total clicks, total products and conversion rate
}